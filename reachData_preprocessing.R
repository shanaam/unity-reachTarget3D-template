## Proprocessing of Raw Reach Data

# Read in data from the hand-tracker output files

# Determine a relevant measure per trial (e.g. angle at max velocity)

# Output a CSv file with per trial data (perhaps also merge the per-trial file made by UxF)